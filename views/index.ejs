<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Whatsapp</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <h1 style="color: white">All chats</h1>
    <form method="get" action="/chat/new"><button>New chat</button></form>
    <br />
    <%for (chat of chats){%>
    <div class="chats">
      <p>from : <%=chat.from%></p>

      <div class="msg">
        <p><i><%=chat.msg%></i></p>
        <form method="get" action="/chats/<%= chat.id %>/edit">
          <button style="border-radius: 8px"><i>edit</i></button>
        </form>
      </div>
      <p>Recived by :<i><%= chat.to %></i></p>

      <p><%= chat.created_at.toString().split(" ")[4] %></p>

      <p><%= chat.created_at.toString().split(" ").slice(0,4).join("-") %></p>
    </div>
    <br />
    <br />
    <%}%>
  </body>
</html>
